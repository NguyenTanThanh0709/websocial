import classNames from 'classnames/bind';
import styles from './Profiles.module.scss';
const cx = classNames.bind(styles);

function Profiles() {
    return ( <h2>Profileseeeeeeeeeeeeee</h2> );
}

export default Profiles;